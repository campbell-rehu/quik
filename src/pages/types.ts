export enum Page {
  Splash = 'Splash',
  Lobby = 'Lobby',
  InGame = 'InGame',
}

export enum Routes {
  Splash = '/splash',
  Lobby = '/lobby',
  InGame = '/ingame',
}

export type BooleanMap = {
  [key: string]: boolean
}
